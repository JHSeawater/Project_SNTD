# 개발 진척

* **현재 구현한 것**:

  * 기본적인 뱀게임 로직
  * 골드, 라이프 로직 및 UI
  * 뱀의 몸통을 따라 적이 머리 쪽으로 이동
  * 상점(타워 건설) 기능
  * 타워 공격 기능
  * 적 웨이브 구현
  * 뱀 게임 일시정지 기능

* **앞으로 구현할 것**

  * 상점(타워 건설) UI 보강
  * 타워 종류, 적 종류, 각종 아이템과 오브젝트 추가
  * 스테이지(맵) 추가
  * 에셋, 이미지, UI 구현
  * 세계관, 스토리 입히기

* **당장 수정이 필요한 버그**

# 개발 로그

* **오후 5:10 2026-01-31(토) - 버그 수정 및 최적화**
  1. **GameManager.cs 수정**: `Awake` 함수에 `Time.timeScale = 1f;` 코드를 추가하여, 게임 오버 후 재시작 시 게임이 멈춰있는 현상을 해결했습니다.
  2. **BuildManager.cs 개선**: `GameObject.Find`를 사용하여 타일맵을 찾는 불안정한 코드를 제거했습니다. 대신 Inspector에서 직접 참조를 할당하도록 변경하고, 누락 시 경고 로그를 출력하도록 했습니다.
  3. **Tower.cs 최적화**: `Physics2D.OverlapCircleAll`을 `Physics2D.OverlapCircleNonAlloc`으로 교체했습니다. 이를 통해 매 프레임 불필요한 메모리 할당(Garbage Collection)을 방지하여 성능을 개선했습니다.